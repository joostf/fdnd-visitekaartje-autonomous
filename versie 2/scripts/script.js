//const expandButton = document.querySelector('header button')
//expandButton.addEventListener('click', expand)

//function expand () {
//document.body.classList.toggle('expand')
//}




const openCardButton = document.querySelectorAll('btn1')
const openCardButton = document.querySelectorAll('btn2')
const openCardButton = document.querySelectorAll('btn3"')

openCardButton.forEach(button => {
  button.addEventListener('click', () => {
    const button = document.querySelector(card_1)
  })
})

openCardButton.forEach(button => {
  button.addEventListener('click', () => {
    const button = document.querySelector(card_2)
  })
})


openCardButton.forEach(button => {
  button.addEventListener('click', () => {
    const button = document.querySelector(card_3)
  })
})